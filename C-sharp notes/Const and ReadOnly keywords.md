
Const keyword should be initialized during compile time. (It should have a value when it is being compiled)

Readonly on the other hand cand have value during the initialization(Compile time) or with Constructor (Runtime).