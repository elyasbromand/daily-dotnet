




## namespace

a namespace is a collection of related classes, interfaces and other related types.

## Assembly (`dll` (dynamic link library), `exe`)

Assembly is collection of namespaces which can be in `dll` or `exe` format.

An assembly is a collection of types and resources that are built to work together and form a logical unit of functionality.

They are building blocks of a .NET application.

